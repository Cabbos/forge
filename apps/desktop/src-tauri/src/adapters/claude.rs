// Phase 6: Claude API adapter (Anthropic API)
// Currently using AnthropicAdapter in adapters/anthropic.rs
pub struct ClaudeAdapter;

impl ClaudeAdapter {
    pub fn new() -> Self {
        ClaudeAdapter
    }
}
