# Stability Regression Batch

Run these tasks against a disposable current project. Controller-side manual writes invalidate the task.
Record each result with the exact command, diff, screenshot, or final-answer evidence used to judge it.

Before rows #1-#3, run `node scripts/disposable-loop-preflight.mjs --json --project /Users/cabbos/project/forge-test-app`. `readyForLoop: true` is required for fresh disposable-project evidence; a dirty or missing project must be fixed or recorded before the live run starts. If preserving residual changes matters, use `node scripts/prepare-disposable-loop-project.mjs --json` to create a clean worktree target instead of resetting the original project. Use `node scripts/phase8-disposable-loop-status.mjs --markdown` to see which rows already have complete archived validation and which row should run next. For each live row, start with `node scripts/phase8-disposable-loop-runbook.mjs --markdown --row <row>` to print the exact prompt, manual JSON path, and collect/validate/archive commands. Generate the manual evidence shape with `node scripts/create-disposable-loop-manual-json.mjs --row <row> --out <file>`, then fill it after Forge finishes. Use `node scripts/collect-disposable-loop-evidence.mjs --markdown --row <row> --run-build` to capture the changed-file, diff, and build/check evidence packet. Use `node scripts/validate-disposable-loop-evidence.mjs --json --row <row> --manual-json <file> --run-build --require-complete` as the hard gate once the manual fields are filled, then `node scripts/archive-disposable-loop-evidence.mjs --json --row <row> --manual-json <file> --run-build --require-complete` to write the evidence files under `apps/desktop/docs/product/evidence/phase8-disposable-loop/`.

| # | Task | Expected Permission State | Required Evidence | Result |
| --- | --- | --- | --- | --- |
| 1 | `/fix @src/App.tsx` for a small visible button feedback issue | Trust or Full Access should avoid repeated routine write prompts | final answer, diff, build/check result | Automated policy evidence passed 2026-06-27: `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml permission_handlers --lib` and `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml harness::permissions --lib` prove Trust allows matching workspace writes, Full Access allows confirmable workspace operations, same-workspace sessions inherit the mode, and the mode syncs into the live session harness before send input. Live agent final-answer/diff/build evidence still follows `apps/desktop/docs/product/phase8-disposable-loop-protocol.md`. |
| 2 | CSS layout polish in current project | Routine write allowed only inside current workspace | changed files, no external write | Automated policy evidence passed 2026-06-27: `harness::permissions` proves routine current-workspace writes are allowed under Trust/Full Access while external writes stay denied; sensitive workspace writes still require Trust confirmation and only Full Access bypasses them. Live CSS polish run still follows `apps/desktop/docs/product/phase8-disposable-loop-protocol.md`. |
| 3 | Build/check command | Safe shell allowed under Full Access | command output summary | Automated policy evidence passed 2026-06-27: `permission_handlers`, `harness::permissions`, and `harness::shell_policy` tests prove Full Access syncs into live sessions, `npm run build`, `npm test`, `cargo test`, and local preview probes are allowed, and catastrophic shell commands remain blocked. Live build/check prompt evidence still follows `apps/desktop/docs/product/phase8-disposable-loop-protocol.md`. |
| 4 | Preview ownership question | final answer states URL and workspace path | final answer + Project Status details | Automated Project Status evidence passed 2026-06-27: expanded delivery details show preview status, URL, preview ownership, and `/Users/cabbos/project/forge`. |
| 5 | `/code-review` | findings-first, calibrated severity | review output | Automated review intent evidence passed 2026-06-27: `cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml capability_context --lib` verifies `/code-review` hidden context leads with findings, reserves P0/P1 for true blockers or unsafe results, treats product gaps as P2, and does not offer fixes unless asked. Manual beta run remains Pass/P2 because earlier output was too aggressive. |
| 6 | New conversation same workspace | runtime trust/full access inherited | Composer mode + getPermissionMode args | Automated inheritance evidence passed 2026-06-27: Project Status trust and Composer Full Access both inherited into a new same-workspace conversation with matching `getPermissionMode` args. |
| 7 | External path write attempt | blocked or confirmed | confirm/deny evidence | Automated UI takeover guard passed 2026-06-27: Full Access and Trust did not auto-approve external-path confirmation cards. |
| 8 | Secret-like path write attempt | blocked or confirmed | confirm/deny evidence | Automated UI takeover guard passed 2026-06-27: Trust did not auto-approve `.env` or `.env.local` workspace confirmation cards. |
| 9 | Restart with active task | honest restore or recovery notice | restart smoke evidence | Mocked restart smoke passed 2026-06-27: `e2e/level3-runtime-restart.spec.ts` restored durable loop, session, A2A, and gateway facts after browser close/reopen. This is partial browser-level evidence, not true Tauri force-quit proof. |
| 10 | Context usage after provider event | `余` means true remaining context | Composer label + provider usage row | Automated context remaining evidence passed 2026-06-27: provider usage `411 / 1M` rendered as `余 999.5K`, not the 967K auto-compact threshold. |
